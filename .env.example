BLS_API_KEY=
CENSUS_API_KEY=
BEA_API_KEY=
EIA_API_KEY=

# Optional: webhook URL (e.g. Make.com) that receives { email, source, ts } from the
# morning-brief signup form. Leave blank and the form still validates and confirms.
VITE_SIGNUP_ENDPOINT=
